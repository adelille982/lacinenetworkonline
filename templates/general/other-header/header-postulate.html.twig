{% if otherHeader %}
<header class="header-postulate">
  <img src="{{ asset('images/en-tete-de-page/autre/' ~ otherHeader.mainImageHeader) | imagine_filter('fullscreen') }}" alt="{{ otherHeader.titleHeader }}" class="img-header-postulate" loading="lazy" />
  <div class="header-postulate-overlay"></div>
  <div class="header-postulate-content">
    <h1 class="title-header-postulate">{{ otherHeader.titleHeader|striptags }}</h1>
    {% if otherHeader.sloganHeader is not empty %}
      <p class="subtitle-header-postulate">{{ otherHeader.sloganHeader|striptags }}</p>
    {% endif %}
  </div>
</header>
{% endif %}
